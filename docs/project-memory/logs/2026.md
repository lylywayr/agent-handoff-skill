# 2026 项目迭代记录

## PC-20260924T082000Z-v110 · 单一 Skill 兼容长期项目连续性
- 日期：2026-09-24（Asia/Shanghai）
- 执行者：OpenMinis/启明星
- 类型/模块：功能扩展；SKILL.md、manifest、README、references、tests、项目事实源
- 基线：本地 `dev@09f0780`；本轮 `fetch` 时远端 `origin/dev@a723d9e`，本地领先 2 个提交
- 版本节点：v1.1.0 文档与技能清单；开发分支 `dev`，未声明正式发布/标签
- 改动：把长期项目初始化、仓库或知识库事实源、定向检索、分层授权、年度日志、远端核验与失败恢复加入原 `agent-handoff`；保留 GitHub Hub 接力为选用模式；同步 README、manifest、旧 Hub 规格适用边界、新增 `AGENTS.md` 冷启动入口和测试。
- 原因与取舍：避免多 Skill 的重复事实和冲突路由；Hub 负责 WIP/交接，项目仓库负责已验证的长期事实。选用协议 + Git 本地探针，未虚称知识库后端自动适配已实现。
- 验证：旧 Hub 4 项本地测试通过；新临时裸仓探针通过（`trunk` 初始提交、远端读回、并发 push 拒绝且本地状态保留）；JSON 语法和 diff-check 通过。真实远端认证、知识库 CAS、完整 Agent 执行未测试。
- 风险/后续：原 Hub 脚本仍仅支持其双分支工作方式；`main` 尚未发布 v1.1.0。提交与远端同步结果见后续验收记录。
- 关联：`SKILL.md` §15、`references/workflow-contract.md`、`references/project-memory-schema.md`；此前 Hub v1.0.x 修复见 Git 历史 `09f0780` 和 `d4dee7c`。
- 状态：初始变更提交与同步结果见下方后续记录。

## PC-20260924T082100Z-sync · 开发提交与远端同步验收
- 日期：2026-09-24（Asia/Shanghai）
- 执行者：OpenMinis/启明星
- 模块：项目事实源、Git 开发分支同步
- 基线：`dev@6da553f` 已包含上条日志；推送后以 `git ls-remote` 验证远端 `dev` 指向 `6da553f`；再显式 fetch 同步本地 `origin/dev`，并核对日志 ID 存在于远端提交。
- 修正：独立复核提示“领先提交数”可能不准，经本地 `git rev-list --left-right --count origin/dev...HEAD`（首次推送前为 `0 2`）和提交链确认起点相对旧远端确为 **2** 个既有本地提交，即 `d4dee7c`、`09f0780`；此项无需改动原基线。首次 `git push` 实际成功，但该命令链后续普通 fetch 的本地追踪引用未更新导致整条命令退出非零；随后显式 fetch+比较 SHA 验收通过，不能把最初退出码当作推送失败。
- 结果：开发提交 `6da553f` 已同步远端 `dev`，并核实原日志 ID；本条补录的状态与 `STATUS.md` 更新需另一次提交与同步。`main` 尚未发布 v1.1.0。
- 风险/后续：授权矩阵、知识库 CAS、跨 Agent 冷启动尚未端到端验收。
