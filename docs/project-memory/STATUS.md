# 当前状态

- 当前版本：v1.1.0 已合并至 `main`；`main@f00d89b` 经推送与远端重新获取核验。`dev` 在合并前为 `74fd776`，后续指针以 Git 远端实时状态为准。
- 已验证：Hub 四项本地回归、非 `dev` 本地裸仓初始化/并发拒推探针、JSON 解析、`git diff --check`；合并后在 `main` 重新运行测试并通过。
- 未验证：真实 GitHub 权限/跨 Agent 冷启动、知识库 CAS、授权矩阵与完整自动执行 E2E。相关规则为操作协议，不宣称已有通用后端程序。
- 发布状态：v1.1.0 已在默认展示分支 `main` 可用；尚未创建 v1.1.0 标签或 GitHub Release。
- 后续：知识库 CAS、授权矩阵、跨 Agent 冷启动与自动执行 E2E 仍待验证；这些限制不因主分支合并而消失。
