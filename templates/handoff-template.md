---
origin: agent
wip-status: none
# 字段说明：
#   origin      = agent | degraded-agent-suggestion（降级 agent 建议）
#   wip-status  = none | pending | absorbed | abandoned
#   仅降级建议额外带 merged: false；普通交接文档省略 merged 字段。
---

# 交接：<任务名>

> 更新于 <UTC ISO-8601 时间戳，如 2026-08-07T13:30:00Z> by <agent 名称> | 状态：进行中 / 已完成 / 阻塞
> seq：<项目内递增序号，仅 INDEX 排序用>
> 交接文件：<与本文件名一致，YYYY-MM-DD-HHmmss-<agent>-<任务>.md>

## 一句话目标
<用一句话说明这个任务最终要达成什么>

## 项目指针
- 仓库：github.com/<用户>/<repo>（私有 / 公开）
- 工作分支：dev（接力现场所在；main 仅放正式可用版本）
- 相关目录：<相关子目录，无则写「根目录」>
- wip 分支：<如有，dev-wip/... 指针 + 状态；无则写「无」>

## 当前进度
- [x] 已完成：<已完成的事项>
- [ ] 未完成：<尚未完成的事项>

## 下一步（最重要）
<接手方最该先做的一件事，尽量具体到可执行>

## 关键决策与理由
<已做过的技术/方案决策，以及为什么——防止下一个 agent 推翻重来>

## 上下文指针
<仅写**跨终端可达**的指针：GitHub 仓库地址、分支名、commit hash、issue·PR 链接、仓库内相对路径、复现命令。超 30 行的日志或代码不内联，指向文件或 commit。
**严禁写**：本机绝对路径（/vol2/...、/home/...、C:\...）、原 agent 的部署/安装位置、原终端环境状态——这些只在你当前这台机器有效，接收交接的 agent 在另一台机器上访问不到。>

## 阻塞与注意事项（可选）
<当前卡点、需要用户或特定环境才能做的事。描述环境需求时写「需要某环境具备 X」，不要写「我这台机器没有 X」——后者对接收方无意义>

## 过程附件（可选）
<如需让接收方了解工作过程，把**提炼后的**关键讨论、被否决方案、中间产物整理成独立文档，存到 hub 本项目文件夹 `notes/` 下，并在此列出文件名。这是提炼后的过程记录，不是对话原文>
